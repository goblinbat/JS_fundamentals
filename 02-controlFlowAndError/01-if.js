// If
let isOn = true;
if(isOn == true) {
    console.log('The light is on')
};

if(isOn) {
    console.log('yay')
};

let isOff = false;
if(isOff==false) {
    console.log('The light is on')
};

/**
 * if (condition) {
 *   run code block
 * };
 */

 let weather = 85;
 if (weather > 72) {
     console.log("it's hot")
 };